# Used by the app in `wails dev` (loaded best-effort from .env)
# Priority: NFC_CONSOLE_X_APP_KEY > X_APP_KEY > embedded build-time key

# Preferred:
NFC_CONSOLE_X_APP_KEY=

# Fallback:
# X_APP_KEY=

# Dev-only: ignore host license constraints in the UI (client-side).
# WARNING: this does not disable server-side checks.
# NFC_CONSOLE_IGNORE_HOST_LICENSE=true
